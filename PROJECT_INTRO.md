# FlowMark Editor

专为 AI 辅助写作场景设计的纯本地离线 Markdown 编辑器，采用 Notion 风格的所见即所得编辑体验。

## 基本信息

| 项目 | 内容 |
|------|------|
| **名称** | FlowMark Editor |
| **类型** | 跨平台桌面端 Markdown 编辑器 |
| **版本** | 1.0.0 |
| **技术栈** | Electron 31 + Vanilla JS + CSS3 |
| **许可证** | MIT |
| **平台支持** | macOS 12+, Windows 10+ |

## 核心特性

### 本地优先
- 完全纯本地离线运行，无云端服务器
- 无自动云同步、无用户数据上传
- 所有文档、图片、配置保存在用户本地文件夹

### 文件透明
- 原生本地文件 IO 操作，直接读写磁盘 .md 文件
- 不加密、不封装、不私有格式
- 文件完全透明可控

### 同步方案
不内置云服务，支持用户自行通过以下方式同步：
- iCloud / OneDrive / Google Drive
- Git (GitHub, GitLab, Gitee)
- NAS / 局域网共享文件夹
- 任意文件同步工具

### 轻量架构
- 单工作区设计，同一时间仅挂载一个本地根文件夹
- 低内存占用，启动速度快
- 无插件市场、无主题商店，架构精简，长期稳定易维护

## 项目结构

```
src/
├── main/
│   ├── main.js       # Electron 主进程
│   │                  #   - 窗口管理
│   │                  #   - IPC 处理器（文件系统操作）
│   │                  #   - 对话框管理
│   │                  #   - 应用菜单
│   └── preload.js    # 预加载脚本
│                      #   - 通过 contextBridge 安全暴露 API
│                      #   - 仅暴露必要的文件系统操作接口
│
└── renderer/
    ├── index.html    # 主页面
    ├── styles/
    │   └── main.css  # 全局样式
    │                  #   - 三套主题（浅色/深色/护眼）
    │                  #   - 组件样式
    │                  #   - 表格调整大小样式
    └── scripts/
        └── app.js    # 渲染进程主逻辑
                        #   - 编辑器（ContentEditable WYSIWYG）
                        #   - 文件树组件
                        #   - 大纲组件
                        #   - 悬浮工具栏
                        #   - 右键菜单
                        #   - 表格操作
                        #   - 主题切换
                        #   - 搜索功能
```

## 主要功能

| 功能 | 说明 |
|------|------|
| **工作区管理** | 选择本地文件夹作为工作区，路径持久化存储 |
| **文件树** | 展示完整目录结构，支持新建/重命名/删除文件和文件夹 |
| **所见即所得编辑** | ContentEditable WYSIWYG 编辑器，实时预览 |
| **斜杠命令** | 输入 `/` 快速插入格式元素 |
| **格式化工具栏** | 选中文字后自动显示，支持粗体/斜体/下划线/删除线/链接/代码/高亮 |
| **快捷键** | Ctrl/Cmd + B/I/U/K 等常用格式化快捷键 |
| **大纲视图** | 自动解析 H1/H2/H3 标题，实时同步，点击跳转 |
| **自动保存** | 编辑后 2 秒自动保存，显示保存状态 |
| **状态栏** | 显示文件名、总字数、当前行号 |
| **右键菜单** | 文件操作、表格操作（插入/删除行列）等 |
| **表格** | 支持插入表格、行列调整、右键菜单操作 |
| **项目搜索** | 工作区内全文搜索，快速定位 |
| **主题切换** | 浅色/深色/护眼三套主题，通过菜单切换 |

### 主题色彩

**浅色主题（默认）**
- 背景: #FFFFFF, #F7F8FA
- 文字: #1A1A1A, #6B7280
- 强调色: #3B82F6

**深色主题**
- 背景: #1E1E1E, #252526
- 文字: #CCCCCC, #858585
- 强调色: #0D7CF0

**护眼主题**
- 背景: #F4ECD8, #EDE4CF
- 文字: #5B4636, #7A6552
- 强调色: #8B7355

## 界面布局

```
┌──────────────────────────────────────────────────────────────┐
│  标题栏 (28px) - macOS 窗口控件区域                           │
├────────────┬─────────────────────────────┬───────────────────┤
│  侧边栏    │       主编辑区              │     大纲栏        │
│  (240px)   │       (flex: 1)             │     (220px)       │
│            │                             │                   │
│  文件树    │   所见即所得编辑            │   H1/H2/H3目录    │
│  + 搜索    │   + 悬浮工具栏             │   点击跳转        │
├────────────┴─────────────────────────────┴───────────────────┤
│  状态栏 (24px) - 文件名 + 字数统计 + 行号                      │
└──────────────────────────────────────────────────────────────┘
```

## 文件格式

- 纯标准 Markdown (.md) 格式
- 图片存储在 `.flowmark-assets/` 隐藏文件夹
- 使用相对路径引用图片
- 无任何私有或加密格式

## 开发

```bash
# 安装依赖
npm install

# 开发模式运行
npm run start

# 构建 macOS 应用
npm run build:mac

# 构建 Windows 应用
npm run build:win
```

## 输出产物

- macOS: `dist/FlowMark Editor-1.0.0-arm64.dmg`
- Windows: `dist/FlowMark Editor-1.0.0.exe`